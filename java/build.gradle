apply from: rootProject.file('jar.gradle')

dependencies {
    compileOnly project(':core')
    compileOnly project(':javalike')
    testImplementation 'junit:junit:4.+'
}

test {
    minHeapSize("512M")
    maxHeapSize("4096M")
}
